# Customer-Lens QA Audit — 2026-08-02 (pre-launch)

Method: served `store/site/` locally and crawled every page (all hrefs/srcs/anchors verified); opened all 3 product xlsx with openpyxl (sheets, formulas, input-cell fills, visible text) and all 4 PDFs (headers, sizes, full visual proofread); cross-checked every price across xlsx/PDF/listing.md/store pages/BROWSER-TASKS; verified the ElevenLabs embed against current documented widget usage and the live agent config; verified the Jotform form; reasoned over the CSS at 375px. Complements `audit-expert-findings.md` (same day) — overlaps are cross-referenced, not repeated.

---

## FIXED in this pass (commit "customer-audit: fixes + findings")

1. **Dead Buy buttons.** All product pages had `href="#"` buy buttons (two per page) — clicking silently jumped to the top of the page. Generator (`store/build_store.py`) now renders an honest disabled state — grey button "Checkout opens this week" + trust line "Payments are being switched on — no orders are taken until checkout is live." — and flips to the real Stripe link + "Secure checkout by Stripe…" line automatically the moment `payment_link` is set. Guard also rejects `#`/PLACEHOLDER values.
2. **`SUPPORT_EMAIL_PLACEHOLDER` visible to customers** in the footer of every store page, the Policies contact section, and the success page. Footer/contact/success now degrade gracefully while `support_email` is empty (footer links to the contact page; contact page explains support opens with checkout and points Etsy/Gumroad buyers to marketplace messages). No placeholder can ever render again.
3. **Receptionist footer placeholder** (`Questions: SUPPORT_EMAIL_PLACEHOLDER`) → replaced with a working "setup form reaches a human" link. (The dead `STRIPE_PAYMENT_LINK_PLACEHOLDER` Subscribe button had already been removed by the expert-audit commit b7c0671.)
4. **Every SVG product card had value text overflowing the white card** by 4–64px (values start-anchored at x=360; card edge at x=390 — "your rules", "25 × $40", "$14.99 value" etc. spilled onto the page background). Affected the three LIVE images (`challenge-pack.svg`, `wedding-budget.svg`, `simple-budget.svg`) and the four archived ones. Fixed: values right-anchored at x=378; verified by rendering before/after.
5. **debt-payoff.png and bundle.png (middle mini-card) made two false claims**: "Every debt, ranked & dated — automatic" and "Interest saved by plan — shown". The actual workbook ranks nothing automatically (strategy order is manual) and has no interest column. Pixel-patched both images (rows now "Snowball vs Avalanche — side-by-side" / "Months to payoff, per debt — calculated"), matching fonts/colors; visually verified. Same rows fixed in `debt-payoff.svg`.
6. **Store copy overclaimed the Debt Payoff Planner**: "Up to 10 debts, ranked automatically both ways" and "List your debts once" — neither is true of the file (see Open #A). Sub and bullets rewritten to what the product actually does.
7. **Savings Goal Tracker FAQ claimed editable "dates"** — the workbook input is "Months left", there is no date field. FAQ now says "names, targets, timelines".
8. **Debt-Payoff-Planner.xlsx START HERE step 3 promised "estimated interest for each strategy"** — no interest figure exists anywhere in the file. Wording fixed in place (openpyxl; before/after integrity check: formulas, fills, merges, freeze panes identical).
9. **Savings-Goal-Tracker.xlsx "My Goals" B4 was nonsense**: "Months from now I'm checking these (enter current month #, e.g. 1):" — it references an input cell that doesn't exist and no formula uses any such value. Replaced with a real explanation of the Months-left column. Same integrity check passed.
10. **No favicon** on store or receptionist (404 + generic tab icon) → inline data-URI SVG favicons added (store: gold $ on navy, in the generator; receptionist: phone glyph).
11. **`build_product.py` docstring used the banned phrase "Budget-by-Paycheck"** (The Budget Mom, LLC's registered method brand per the listing.md IP screen) → renamed, warning comment added so it never leaks into copy.
12. **`build_bump_pack.py` comment** described biweekly deposits as "$3,$9,…" (code uses $6 steps; the PDFs are correct) → comment fixed.
13. **Nav CSS**: `margin-left` per link → flex `gap` (clean wrap at 375px; no stray indent when nav wraps under the brand).
14. Unused `shutil` import removed from the generator.

## VERIFIED OK (checked, nothing to fix)

- **Links/anchors/assets**: all 10 store pages crawled — every internal href/src resolves; all policy anchors (`#refunds #delivery #terms #privacy #contact`) and receptionist anchors (`#demo #pricing`) exist. Zero placeholders remain anywhere in `store/site/` or `receptionist/` (swept for PLACEHOLDER/lorem/TODO/willowpine).
- **xlsx**: all sheets in all 3 workbooks load; yellow input cells ARE marked everywhere (stored as ARGB `00FFF2CC` — an openpyxl storage quirk; Excel/Sheets ignore the alpha byte and render the yellow correctly). Example math verified: Left-to-Assign $0; snowball months 6/75/34; Aug savings rate 8.3%; goal $/mo 215/167/175 match the card image; Savings Log total range matches the yellow input rows exactly (no off-by-one).
- **PDFs**: 4 valid single-page PDFs (v1.4, 300dpi, 308–475KB), rendered and proofread — totals correct ($1,378 / $2,106 / 25×$40 / 31-day grid), no overlaps or typos.
- **Prices consistent everywhere**: store $14.99 / $14.99 / $12.99 / $9.99 / $29.99 / $16.99 / $9.99 = listing.md Etsy prices = TASK-002 Stripe amounts; bundle compare-at $42.97 = exact sum; "Save $12.98 — 30%" = 30.2% ✓. (The earlier false "less than the price of two" line was fixed in b7c0671.) Gumroad twin prices are intentionally lower per listing.md — see Open #F.
- **ElevenLabs embed** matches the currently documented pattern exactly (`<elevenlabs-convai agent-id>` + `https://unpkg.com/@elevenlabs/convai-widget-embed` async script). Live agent verified via API: exists, public, `enable_auth:false` (required for embeds), greeting matches the page's "Summit Home Services / Ava" copy.
- **Jotform** `262131333242039` is ENABLED, titled "AI Receptionist Setup — Tell Us About Your Business", 15 questions.
- **375px**: hero/product grids collapse via `auto-fit/minmax`, `.pd` goes single-column ≤760px, buttons go block on mobile, images are `width:100%`; no fixed widths, no horizontal-overflow risks found on store or receptionist.

## OPEN — needs owner or product decision

- **A. Debt Payoff Planner is thinner than its (now-corrected) marketing**: "My Debts" holds 10 debts, but the Snowball/Avalanche tabs contain independent, static 5-row copies — debts are re-entered per tab, order is manual, months formulas cover 5 rows only. Copy has been trued-up; the right fix is a v2 workbook (strategy tabs formula-linked to My Debts, 10 rows). Note: unlike the paycheck product, this xlsx has **no committed generator script** — add one when building v2.
- **B. Willowpine remnants / brand split (highest-risk open item).** `BROWSER-TASKS.md` TASK-002–009 still instruct desktop Claude to create the public Pinterest/Fiverr/Meta/Etsy/Gumroad/MailerLite presence as "Willow & Pine Studio" / "WillowAndPineStudio" / "willowpinestudio", and TASK-002 sets Stripe statement descriptor `WILLOWPINE` (also in `venture/06-account-setup/round-2-additions.md`) — while the store, the queued domain (thepaycheckbudget.com), and policies.html ("Charges appear as THE PAYCHECK BUDGET on your statement") all say The Paycheck Budget. Executed as written, a buyer sees ad → store → bank statement in different brands = confusion + dispute risk. Not edited here because some tasks may already be executed under the old names (results doc not readable from this session). Needs one deliberate rebrand pass over the task queue; suggested descriptor `THEPAYCHECKBUDGET` (17 chars, fits Stripe's 5–22 limit).
- **C. No payment links queued for the two new products** (wedding-budget-planner $16.99, simple-budget-starter $9.99) — TASK-002 predates them and creates only 5 links. Their buy buttons will correctly show "Checkout opens this week", but will stay that way until two more Payment Links are queued and wired.
- **D. "Every goal gets a number, a date, and a plan"** (savings page headline + paycheck/savings/bundle PNG imagery) vs. the workbook's "Months left" input. Months-left is a timeline, not a date — defensible, but the clean fix is a real target-date column in v2, after which copy and images align perfectly. (FAQ's harder claim already fixed, item 7.)
- **E. Source SVGs in the two new product folders** (`wedding-budget-planner-v1/store-card.svg`, `simple-budget-starter-v1/store-card.svg` — other agents' folders, deliberately untouched) still contain the value-overflow bug fixed in the deployed copies; if recopied to `store/site/img/` they reintroduce the glitch.
- **F. Cross-platform price gap is intentional but visible**: Gumroad twins are $2 cheaper ($12.99/$14.99/$8.99 per listing.md; TASK-008 consistent). A shopper who finds both storefronts will notice. Keep deliberately or align — just deciding is the fix.
- **G. EverAnswer naming conflict** (expert audit P0-1) remains open; the placeholder fixes here don't change that page's launch-readiness.
- **H. Unconditional Meta Purchase event on success.html** whenever a pixel ID is set (expert audit P1-5): anyone opening the URL directly inflates conversions; the value/currency patch there is still pending.
- **I. Support email still unset** (wave-1 pack recommends a dedicated shop Gmail). Pages degrade gracefully now, but set `CONFIG["support_email"]` in `store/build_store.py` and rerun it the day the inbox exists.
